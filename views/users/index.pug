h1 List Name
form(action="/users/search", method="get")
    input(type="text", name="q")
    button search
ul
    each user in users
        li
            a(href='/users/' + user.id)= user.name
            